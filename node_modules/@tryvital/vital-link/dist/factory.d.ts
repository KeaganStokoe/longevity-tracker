import { VitalLinkOptions } from './types';
export interface VitalFactory {
    open: Function;
    exit: Function;
    destroy: Function;
}
/**
 * Wrap link handler creation and instance to clean up iframe via destroy() method
 */
export declare const createVital: (options: VitalLinkOptions) => {
    open: (token: string) => void;
    exit: (exitOptions: any, callback: Function) => void;
    destroy: () => void;
};
